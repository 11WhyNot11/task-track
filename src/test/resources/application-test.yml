spring:
  datasource:
    url: jdbc:tc:postgresql:15:///testdb
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: false

jwt:
  secret: testsecret
  expiration: 3600000